/* vector<int> a[n];: 
    This declares an array named a of n vectors of integers. 
    Each element of the array a is itself a vector. 
    The size of each vector is not specified during declaration; 
    instead, each vector is initially empty.
vector<int> a(n);: 
    This declares a single vector named a of integers with n elements initialized to their default value,
     which for integers is zero.
 */
#include<bits/stdc++.h>
using namespace std;

int main(){

    int n,k; cin>>n>>k;

    vector<int> a(n);

    return 0;
}